<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Wi-Fi State Inspection: Needed to query Wi-Fi status, link speed, and connection state -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Wi-Fi State Control: Needed to control Wi-Fi P2P channel and trigger peer discovery -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Network State: Needed to query ConnectivityManager for active network capabilities and IP routes -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Local Socket Networking: Needed to bind local TCP and UDP sockets for control and RTP reception -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Multicast Lock: Needed for mDNS / DNS-SD reception in MS-MICE mode -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Legacy Location Permissions (API <= 32): Android 12 and earlier require Fine and Coarse Location for P2P discovery -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="32" />

    <!-- Nearby Wi-Fi Devices (API 33+ / Android 13 & 14): Required for Wi-Fi Direct on modern Android -->
    <!-- neverForLocation flag informs the OS that Wi-Fi Direct is not used to track physical location -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="tiramisu" />

    <!-- Foreground Service Permissions: Keeps wireless display session alive during app switches and rotation -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pad2WirelessDisplay"
        android:hardwareAccelerated="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Pad2WirelessDisplay"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground Service hosting the Miracast / MS-MICE RTSP, RTP and Video Decoding Engine -->
        <service
            android:name=".service.SecondScreenService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|mediaPlayback" />
    </application>

</manifest>
